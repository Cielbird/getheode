use crate::phonology::rule::parse::parsed::{ParsedRule, StrictParsedRule};

/// enumerate the possibilities in each branch of each tree of inputs and context for each rule.
/// eliminates all branch nodes. 
pub fn enumerate_branches(rule: ParsedRule) -> Vec<StrictParsedRule> {
    todo!()
}

